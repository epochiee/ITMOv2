# R.C.T.F.

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- **Role:** Инженер по надёжности API. Уточняет проверяемость и соответствие SLA.
- **Context:** Практика 1: problem.md (SLA: P50 <= 2s, P95 <= 5s), tests_load.md текущие сценарии, ADR лимит diff.
- **Task:** Сформулировать проверяемые критерии для сценария «Большой diff» и синхронизировать их с problem.md.
- **Format:** Короткая замена строки в таблице, без изменения структуры файла.

## Полный запрос

Уточни в одной строке сценарий «Большой diff 60 KB» в practices/practice_01/tests_load.md так, чтобы допуски были проверяемыми и согласованы с SLA из problem.md: явно укажи порог P95, целевой процент 2xx, длительность, что измеряем и правило остановки. Не меняй стиль таблицы.

## Что получили

- Ответ с явными порогами: «P95 <= 5s; >= 99% 2xx; стоп при error rate > 1%; длительность 10 мин; измеряем P50, P95; распределение статусов».

## Что изменили в исходном артефакте

 - Файл и раздел: practices/practice_01/tests_load.md, таблица «Нагрузочные проверки», строка «Большой diff 60 KB»
 - Изменение: обновили строку «Большой diff 60 KB» по предложенным порогам.
 - Как проверили: make step2; сверка с problem.md.
 - Что отклонили: любое упоминание внешних инструментов за пределами «k6/locust» в колонке «Что измеряем».
