# Few-shot

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Артефакт Практики 1: practices/practice_01/tests_load.md
- Что хотим улучшить: согласованность метрик и порогов со SLA из problem.md; устранить расплывчатые формулировки.

## Примеры

### Хороший результат

Большой diff 60 KB (<= лимит ADR) | 2 RPS, 10 мин | P95 <= 5s; >= 99% 2xx | P50, P95; распределение статусов; стоп при error rate > 1% | problem.md (SLA)

### Плохой результат

Большой diff 60 KB | 2 RPS, 5 мин | Без деградации > 5s | Время ответа, ошибки | Контекст problem.md

## Запрос

Перепиши строку сценария нагрузочного теста для большого diff так, чтобы критерии были воспроизводимыми и согласованными с SLA из practices/practice_01/problem.md. Дай два коротких примера: хороший и плохой. Используй стиль таблицы из tests_load.md.

## Что получили

Результат: заменить «Без деградации > 5s» на явные пороги P95 <= 5s с требованием >= 99% 2xx. Уточнить длительность до 10 минут, добавить правило остановки при error rate > 1% и измерения P50/P95 с фиксацией распределения статусов.

## Что изменили в исходном артефакте

- Файл и раздел: practices/practice_01/tests_load.md, таблица «Нагрузочные проверки», строка «Большой diff 60 KB».
- Изменение: заменили размытый предел на «P95 <= 5s; >= 99% 2xx», синхронизировали длительность и измерения; уточнили ссылку на problem.md (SLA).
- Как проверили: make step2 и визуальная проверка на согласованность с problem.md.
- Что отклонили: внешние источники для SLA.
